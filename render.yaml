services:
  - type: web
    name: maploom-flask
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app --bind 0.0.0.0:$PORT
    envVars:
      - key: FLASK_SECRET
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.11"
